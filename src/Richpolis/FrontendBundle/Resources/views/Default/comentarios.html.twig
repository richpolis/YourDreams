<div class="show-dream comentario-dream comentario-dream-nivel-{{comentario.nivel}}">
    <div class="comentario">
        <h3 class="titulo-comentario">{{comentario.usuario.nombreCompleto}}</h3>
        <span class="fecha-comentario">{{ dream.createdAt|date('d M Y') }}</span>
        <p class="mensaje-comentario">
            {{ comentario.message }}
        </p>
        <div class="acciones-comentario">
            {% if comentario.usuario.id != app.user.id %}
                <a href="#test-popup" class="crearComentario" data-url="{{path('create_dream_mensaje',{'id': comentario.dream.id })}}" data-parent="{{comentario.id}}">
                    responder
                </a>
            {% endif %}
        </div>
    </div>
</div>
{% if comentario.children | length > 0 %}  
    {% for child in comentario.children %}
        {% include 'FrontendBundle:Default:comentarios.html.twig' with { 'comentario': child } %}
    {% endfor %}
{% endif %}        
